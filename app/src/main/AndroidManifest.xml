<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CSCI310_StudyBuddy_Team61_FinalRepository"
        tools:targetApi="31">

        <!-- Declare MainActivity as the entry point -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".CoursesActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".GroupsActivity" />
        <activity android:name=".CourseHomeActivity" />
        <activity android:name=".GroupCalendarActivity" />
        <activity android:name=".StudySessionsActivity" />
        <activity android:name=".ResourcesActivity" />
        <activity android:name=".StudySessionDetailsActivity" />
        <activity android:name=".CreateGroup" />
        <activity android:name=".ChatSetupActivity" />
        <activity android:name=".GroupChatActivity" />
        <activity android:name=".PrivateChatActivity" />
        <activity android:name=".AddStudySessionActivity" />




    </application>

</manifest>
